export default function ComponenteLento() {
  return (
    <section>
      <h2>Módulo Carregado Dinamicamente</h2>
      <p>Este componente foi separado do bundle principal via React.lazy.</p>
    </section>
  );
}